def():
    if n < 0:
        cut = -n
    else:
        cut = len(nums) - n
        list1 = nums[cut:]
        list2 = nums[:cut]
        return list1 + list2

def():
    if n < 0:
        cut = -n
    else:
        cut = len(nums) - n
        list1 = nums[cut:]
        list2 = nums[:cut]
        return list1 + list2

def():
    if n < 0:
        cut = -n
    else:
        cut = len[nums] - n
        list1 = nums[cut:]
        lits2 = nums[:cut]
        return list1 + list2
